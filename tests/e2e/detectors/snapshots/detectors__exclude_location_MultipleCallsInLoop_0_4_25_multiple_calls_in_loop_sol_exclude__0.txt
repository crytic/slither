CallInLoop.bad() has external calls inside a loop: destinations[i].transfer(i)

CallInLoop.bad2() has external calls inside a loop: destinations[i].transfer(i)

CallInLoop.bad3_internal(address,uint256) has external calls inside a loop: a.transfer(i)

CallInLoopBase.bad_base() has external calls inside a loop: destinations_base[i].transfer(i)

